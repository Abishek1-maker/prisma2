//under project this are data which should be encrypted in out jwt in here we want userId under sub value can be retrieved if we have secret key
//step:3 created manuallly
export type AuthJwtPayload = {
  sub: number;
};
